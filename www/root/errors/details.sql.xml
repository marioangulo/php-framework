<?xml version="1.0" encoding="utf-8"?>
<sql>
    <commands>
        <command id="get-record">
            SELECT
                timestamp_created,
                request_url,
                application,
                source,
                error_message,
                debug_log,
                stack_trace,
                request_get,
                request_post,
                request_session,
                request_cookie,
                environment_variables
            FROM error
            WHERE id = '#id#'
        </command>
    </commands>
</sql>
